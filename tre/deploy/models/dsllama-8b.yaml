apiVersion: v1
kind: Service
metadata:
  name: dsllama-8b
  namespace: default
  labels:
    model.aibrix.ai/name: dsllama-8b
spec:
  selector:
    model.aibrix.ai/name: dsllama-8b
    tre.aibrix.io/routable: 'true'
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
