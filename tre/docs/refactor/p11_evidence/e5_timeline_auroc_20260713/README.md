# E5: signal AUROC for violation ranking (RQ1), full recompute 2026-07-13

Generated by `deploy/scripts/analysis/e5_timeline_auroc.py` (tests in
`deploy/tests/test_e5_timeline_auroc.py`) with `--lead-windows 3` over three
evidence roots: `e1/` = canonical_rerun_20260715 (18 runs; note its APA-arm
actuation was broken but its counterfactual signals remain valid for ranking),
`apa_v2/` = canonical_e1_apa_v2_20260712 (9 runs, fixed APA), `noise/` =
noise_seeds_20260716 (12 runs). Per-root `aligned_*.csv`, `summary.csv`,
`summary.json`. Macro AUROC = mean over non-single-class (run x model) cells.

## Macro AUROC (violation propensity; higher = better ranking)

| dataset (cells) | z_m | queue_len | decode_tps | prefill_tps |
| --- | ---: | ---: | ---: | ---: |
| e1 (34) | 0.814 | 0.817 | 0.721 | 0.718 |
| apa_v2 (17) | 0.823 | 0.829 | 0.721 | 0.718 |
| noise (23) | 0.786 | 0.781 | 0.731 | 0.694 |

Lead-3 (15 s early-warning labels): e1 z_m 0.795 vs queue_len 0.778;
apa_v2 0.802 vs 0.811; noise 0.760 vs 0.768.

## Conclusions

1. z_m and queue_len are statistically indistinguishable as PER-MODEL violation
   rankers (each leads on some datasets, all within ~0.005-0.02); both clearly
   beat the throughput signals (~0.07-0.10 gap, consistent everywhere).
2. The E1-only lead-3 edge for z_m does not replicate across datasets and should
   not be claimed.
3. z_m's distinctive, defensible value is CROSS-MODEL comparability driving
   allocation under contention — the E2 t5 result (zm beats decode_tps by
   4.58pp, beyond the 1.71pp TRE noise floor) and the t5 TRE-vs-APA mechanism —
   not a per-model AUROC advantage over queue_len.
